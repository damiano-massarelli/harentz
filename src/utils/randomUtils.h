#ifndef RANDOM_H
#define RANDOM_H

/** \brief returns a random float number in [start, end)
  * \param start first possible value (included)
  * \param end final value (not included)
  * \return a value in [start, end) */
float randRange(float start, float end);

#endif // RANDOM_H
